<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Luxora</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" /> <!---->
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="font-['helvetica_now_display']">
    <nav class="w-full fixed top-0 left-0 px-5 py-3 bg-red-950 shadow-md shadow-yellow-600">
        <h3 class="text-3xl text-amber-500 font-bold flex ">Scatch</h3>
        <% var loggedin = typeof loggedin !== "undefined" ? loggedin : true %> <!--letting the navber to be dispalayed only when the user is loggenin-->
        <% if (loggedin) { %>
            <div class="flex  gap-5 text-white font-semibold items-start">
                <a href="/shop">Shop</a>
                <a href="/cart">Cart</a>
                <a class="" href="/account">Profile</a>
                <a class="text-red-600 font-semibold" href="/users/logout">Logout</a>
            </div>
        <% } %>
    </nav>
</body>
</html>
